# Demo Data Generation — Implementation Tracker

> Keep this updated as work continues. Don't start a "pending" task whose
> "Depends on" column names an unfinished task.

## Completed

### Session 1 — demo account, leagues, games, video/highlight fields

| Task                                                                | Notes                                                                                                                                                                                             |
| ------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Add `require.main === module` guard + `module.exports` to `seed.js` | Exposes `randomInt`, `buildPlayerBlueprints`, `buildLeagueRosterSnapshot`, `buildLeagueGameEvents`, `attachTeamSide` for reuse without triggering `pnpm seed`'s destructive reset on `require()`. |
| Create `server/src/scripts/seed-demo-account.js`                    | New standalone, additive, idempotent generator.                                                                                                                                                   |
| Demo user upsert (`testuser@gmail.com` / `password1!2@3#`)          | Handles the case where the account already existed by updating credentials/plan in place — see `DECISIONS.md`.                                                                                    |
| 3 leagues / 5 `LeagueTeam`s / 8 `LeaguePlayer`s each                | Verified via direct DB query: all three leagues have exactly 5 teams and 40 players each.                                                                                                         |
| Player-profile claims for the demo user (≥2 required)               | 3 profiles claimed (one per league), spanning 3 distinct leagues/teams — exceeds the ">1" requirement.                                                                                            |
| `LeagueTeamMember` role upserts                                     | Verified roles: `player` (Demo League), `manager` (Harborview Rec League), `player` (Summit City Hoops Circuit).                                                                                  |
| Per-team game counts ≥3                                             | 8-game schedule per league (`FIVE_TEAM_SCHEDULE`) verified to give every team ≥3 games.                                                                                                           |
| `videoUrl` rotation across the 4 supplied YouTube links             | Verified 4 distinct URLs used per league, no null `videoUrl` on any seeded game.                                                                                                                  |
| `videoTimestamp` injection on highlight-eligible events             | Verified: 225/225 highlight-eligible events on a sample game carry a numeric `videoTimestamp`.                                                                                                    |
| `seed:demo` npm script added to `server/package.json`               | `pnpm --filter server seed:demo`.                                                                                                                                                                 |
| Idempotency re-run                                                  | Ran the script twice against the dev Atlas DB; second run reported 0 new users/leagues/teams/players/games created.                                                                               |
| Service-layer verification                                          | Called `getMyLeagueProfiles` and `getGameForUser` directly (real application code, not just raw DB reads) — both returned correct, fully-populated data.                                          |
| Documentation folder                                                | `docs/demo-data-generation/{README,TRACKER,DECISIONS}.md` written.                                                                                                                                |
| Committed to git                                                    | `feat(server): add idempotent demo account seed script` (commit `e44eabb`).                                                                                                                       |

### Session 2 — dev DB reset, multi-user Pulse feed content

| Task                                                                                     | Notes                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Create `server/src/scripts/reset-dev-database.js`                                        | Dev-only, guarded (refuses on `NODE_ENV=production` or a non-dev-looking `MONGO_DB_NAME`). Drops collections one at a time (the Atlas app user lacks `dropDatabase` privileges — discovered when the first attempt errored with `AtlasError: user is not allowed to do action [dropDatabase]`).                                                                                                           |
| `db:reset-dev` npm script added                                                          | `pnpm --filter server db:reset-dev`.                                                                                                                                                                                                                                                                                                                                                                      |
| Wiped the dev Atlas database                                                             | Ran with explicit user confirmation after surfacing that it contained more than disposable seed data — a real imported league (`we-ball-saturday`), a personal manual-testing league (`dev-test-league`), and several real-looking user accounts with standalone teams. All of that is now gone; see `DECISIONS.md`.                                                                                      |
| Re-ran `seed-demo-account.js` against the clean DB                                       | Regenerated the full demo dataset (3 leagues, 5 teams/8 players each, 8 games/league) from scratch; verified identical shape to session 1's data.                                                                                                                                                                                                                                                         |
| Added 4 synthetic Demo League teammate users                                             | `demo-teammate-1..4@tsw.demo`, each claimed onto a distinct `LeaguePlayer` slot in Demo League, so posts can be attributed to more than one account.                                                                                                                                                                                                                                                      |
| Renamed `claimPlayerForDemoUser` → `claimPlayerForUser(leaguePlayer, userId, userLabel)` | Bug fix: the original hardcoded "demo user" in its log lines even when called for a teammate, which was misleading (functionally correct, but confusing to read).                                                                                                                                                                                                                                         |
| ≥20 `highlight_clip` posts across multiple games and posters                             | First implementation had a bug — it exhausted the first game's ~225 highlight-eligible events before ever moving to the next game, so all 20 highlights came from one game. Fixed with a round-robin queue (one candidate event per game per pass) before generating for real; verified 20 highlights now span all 8 Demo League games and all 5 available posters.                                       |
| Supporting `image`/`game_card`/`player_card`/`team_card` posts                           | 9 additional posts (5 image, 2 game_card, 1 player_card, 1 team_card), also spread across posters via the same deterministic `pickPoster` helper.                                                                                                                                                                                                                                                         |
| Idempotency re-run (full extended script)                                                | Ran twice; second run reported "already have 20 highlight + 9 other posts, skipping" and made zero writes.                                                                                                                                                                                                                                                                                                |
| Service-layer verification of feed content                                               | Called `listFeedPosts` directly (the function `GET /api/v1/feed` calls) — all 29 posts resolved correctly: `highlight_clip` YouTube URLs pass `isSafeYouTubeUrl`, `game_card`/`player_card`/`team_card` `cardSnapshot`s live-compute correctly (stats, colors, names) via the existing OPT-017 compute-on-miss fallback.                                                                                  |
| Confirmed `FeedPage.jsx`/client feed needs no changes                                    | `FeedList.jsx` already dispatches all six post types to both normal and full-screen renderers (`FullScreenGameCard`, `FullScreenPlayerCard`, `FullScreenTeamCard`, `FullScreenHighlightClipPost` all pre-existing); seeding the right `Post` documents was sufficient. Cloudinary upload was explicitly ruled out in favor of the existing YouTube-based `highlight_clip` post type — see `DECISIONS.md`. |
| Documentation updated                                                                    | `README.md`/`TRACKER.md`/`DECISIONS.md` all updated for this session's work.                                                                                                                                                                                                                                                                                                                              |

### Session 3 — raise highlight target to 50, PROJECT-KNOWLEDGE.md update

| Task                                                | Notes                                                                                                                                                                                                                                    |
| --------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Raised `MIN_HIGHLIGHT_POSTS` from 20 to 50          | Confirmed with the requester that "50 different highlight videos" means 50 distinct highlight posts (each a `(videoUrl, videoTimestamp)` pair), not 50 different YouTube uploads — only 4 source URLs exist.                             |
| Confirmed ~10-second clip duration needs no changes | `HighlightClipPostCard.jsx`/`FullScreenHighlightClipPost.jsx` already embed each clip as `videoTimestamp − 5s` to `videoTimestamp + 5s` — this logic pre-existed the seeding work, so raising the post count was the only change needed. |
| Cleared `Post` collection and regenerated           | Verified 50 highlight posts spread 6–7 per game across all 8 Demo League games, 5 distinct posters, zero duplicate `highlightClip.eventId` values.                                                                                       |
| Idempotency re-run                                  | Confirmed a second run reports "already have 50 highlight + 9 other posts, skipping" with zero writes.                                                                                                                                   |
| Updated `docs/PROJECT-KNOWLEDGE.md`                 | Added a "Demo account seeding" bullet to §10 (Important workflows), a `demo-data-generation/` companion-docs link at the top of the file, and a "Where to start" table row in §12 pointing here.                                         |
| Updated `README.md` highlight-count references      | 20 → 50 throughout, plus the "different videos means different clips, not different uploads" clarification.                                                                                                                              |

### Session 4 — narrow `server/src/scripts/` to only what's needed

| Task                                                                                                                      | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Removed `server/src/scripts/seed-we-ball-saturday.js` + `we-ball-game-1.tsv` / `we-ball-game-2.tsv`                       | Unrelated, pre-existing real-league data importer — not a dependency of `seed-demo-account.js`. Explicitly confirmed with the requester before deleting (it's the one file in the folder that wasn't part of this feature at all).                                                                                                                                                                                                                                                                       |
| Removed `server/src/scripts/reset-dev-database.js`                                                                        | The one-time dev-DB-wipe helper from Session 2. Confirmed with the requester that it's no longer needed going forward — `seed-demo-account.js` is additive/idempotent and doesn't require a clean slate to run correctly, so keeping a standing "wipe the whole DB" script around wasn't worth the risk.                                                                                                                                                                                                 |
| Removed `"seed:we-ball"` and `"db:reset-dev"` from `server/package.json`                                                  | Matches the deleted files; `"seed"` and `"seed:demo"` remain.                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **Did NOT remove** `backfill-*.js` (4 files) or `migrate-drop-dead-indexes.js`                                            | Initially deleted along with everything else in a first pass, then restored after checking `docs/application-audit/000-OPTIMISATION-TRACKER.md` — these are live, still-referenced production tooling (`OPT-007`'s next step is literally "run `migrate-drop-dead-indexes.js`"; `backfill-game-finalscore.js` is listed as a production gating requirement). Deleting them would have removed real, unfinished optimization work, not demo-seeding cruft. Caught before committing — see `DECISIONS.md`. |
| Verified `seed-demo-account.js` still works                                                                               | Lint clean; `--dry-run` against the existing dev DB still correctly detects all 3 leagues/users as already existing, confirming its dependency on `seed.js` (kept) is intact.                                                                                                                                                                                                                                                                                                                            |
| Updated `README.md`/`PROJECT-KNOWLEDGE.md` to drop stale `reset-dev-database.js`/`db:reset-dev`/`seed:we-ball` references | Reframed as removed-but-historically-noted rather than deleting the history from `TRACKER.md`/`DECISIONS.md` (which should stay accurate to what actually happened in each session).                                                                                                                                                                                                                                                                                                                     |

## Pending

| Task                                                                                                                                                        | Depends on                                  | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Browser-based UI walkthrough (login, `/my-sporty`, league pages, Pulse feed scroll on mobile viewport to see full-screen cards, game replay/highlights tab) | —                                           | Not blocked, just not done — Playwright MCP browser access was unavailable in both sessions so far. Service-layer verification substituted for this and is equally conclusive for correctness, but a visual pass (especially the mobile full-screen snap-scroll feed) is worth doing before an external demo.                                                                                                                                                                                               |
| Re-import or recreate `we-ball-saturday` league data if still needed in dev                                                                                 | Someone deciding it's needed                | The league data was deleted by the Session 2 dev DB reset, and as of Session 4 the importer script (`seed-we-ball-saturday.js`) and its source TSV files (`we-ball-game-1.tsv`, `we-ball-game-2.tsv`) are also gone from `server/src/scripts/`. Recreating this league now would require restoring those 3 files from git history (`git log -- server/src/scripts/seed-we-ball-saturday.js`) first, then re-running it — the original owner account would also need to exist again, e.g. via normal signup. |
| Run `backfill-league-standings.js` against the seeded leagues                                                                                               | Demo data creation (done)                   | Optional — compute-on-miss already serves correct standings live.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Decide & schedule the actual production run of `seed-demo-account.js`                                                                                       | Stakeholder sign-off on the demo data shape | The script is production-ready (env guard, `--dry-run`, idempotent) but has only been run against the dev Atlas DB so far.                                                                                                                                                                                                                                                                                                                                                                                  |

## Blocked

None currently.

## Future improvements

- Add a lightweight Jest test for `seed-demo-account.js`'s pure functions
  (`breakTieIfNeeded`, `injectVideoTimestamps`, `pickPoster`) if this script
  is expected to evolve further — currently verified only by manual DB
  inspection and service-layer calls.
- Consider seeding feed content for the other two leagues too (Harborview
  Rec League, Summit City Hoops Circuit), not just Demo League, if a future
  requirement wants the whole demo account's feed activity to look active,
  not just Demo League's.
- Consider adding standalone (non-league) team/player demo data if a future
  requirement wants the demo account to also showcase the "one tracked team
  per standalone game" product mode, which this script currently doesn't
  touch.
- If real Cloudinary-hosted video highlights are wanted later (as originally
  scoped before being descoped to YouTube-only — see `DECISIONS.md`), that
  would need either sourcing freely-licensed direct-mp4 sample clips for
  remote-URL upload, or building a genuinely new video file and uploading it
  via `uploadVideoBuffer` — both out of scope for this iteration.

## Dependencies between tasks

Historical (as it happened — `reset-dev-database.js` no longer exists as of
Session 4, see above):

```
seed.js exports ──▶ seed-demo-account.js (leagues/games) ──▶ dev DB run ──▶ idempotency re-run
                                                             └─▶ service-layer verification
reset-dev-database.js ──▶ clean dev DB ──▶ seed-demo-account.js re-run (leagues/games)
                                                             └─▶ Demo League teammates + feed posts
                                                                  └─▶ feed service-layer verification
                                                                  └─▶ documentation
dev DB run (verified) ──▶ production run (pending stakeholder sign-off)
```

Current (going forward — `seed-demo-account.js`'s only file dependency is
`seed.js`):

```
seed.js exports ──▶ seed-demo-account.js (users/leagues/teams/players/games/feed) ──▶ dev DB run
                                                                                       └─▶ idempotency re-run
                                                                                       └─▶ documentation
dev DB run (verified) ──▶ production run (pending stakeholder sign-off)
```
